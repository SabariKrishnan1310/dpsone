version: '3.9'

services:
  db:
    image: postgres:16-alpine
    container_name: sterlingone_postgres_db
    environment:
      POSTGRES_DB: sterlingone_db
      POSTGRES_USER: sterlingone
      POSTGRES_PASSWORD: SterlingONE
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data/
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready -U sterlingone" ]
      interval: 5s
      timeout: 5s
      retries: 5

  redis:
    image: redis:7-alpine
    container_name: sterlingone_redis_cache
    ports:
      - "6379:6379"

  ingestion_api:
    build:
      context: ./service_ingestion
      dockerfile: Dockerfile
    container_name: sterlingone_ingestion_api
    command: uvicorn main:app --host 0.0.0.0 --port 8000
    ports:
      - "8000:8000"
    depends_on:
      redis:
        condition: service_started
      db:
        condition: service_healthy
    environment:
      POSTGRES_HOST: db
      REDIS_HOST: redis
      POSTGRES_DB: sterlingone_db
      POSTGRES_USER: sterlingone
      POSTGRES_PASSWORD: SterlingONE

  management_web:
    build:
      context: ./service_management
      dockerfile: Dockerfile
    container_name: sterlingone_management_web
    command: daphne -b 0.0.0.0 -p 8001 sterlingone_core.asgi:application

    ports:
      - "8001:8001"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_started
    environment:
      DJANGO_SETTINGS_MODULE: sterlingone_core.settings
      POSTGRES_HOST: db
      REDIS_HOST: redis
      POSTGRES_DB: sterlingone_db
      POSTGRES_USER: sterlingone
      POSTGRES_PASSWORD: SterlingONE

    volumes:
      - ./service_management/staticfiles:/usr/src/app/staticfiles

  management_worker:
    build:
      context: ./service_management
      dockerfile: Dockerfile
    container_name: sterlingone_management_worker
    command: celery -A sterlingone_core worker -l info
    depends_on:
      - db
      - redis
    environment:
      DJANGO_SETTINGS_MODULE: sterlingone_core.settings
      POSTGRES_HOST: db
      REDIS_HOST: redis
      POSTGRES_DB: sterlingone_db
      POSTGRES_USER: sterlingone
      POSTGRES_PASSWORD: SterlingONE

  management_listener:
    build:
      context: ./service_management
      dockerfile: Dockerfile
    container_name: sterlingone_management_listener
    command: python worker_listener.py
    depends_on:
      - redis
      - management_worker
    environment:
      DJANGO_SETTINGS_MODULE: sterlingone_core.settings
      REDIS_HOST: redis


volumes:
  postgres_data: